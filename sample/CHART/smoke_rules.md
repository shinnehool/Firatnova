# Smoke 诊断规则

## 目标
- Smoke 不只是跑通，还要承担诊断职责。
- 重点验证：
  1. 通路是否打通。
  2. loss 是否收敛。
  3. 学习率调度是否异常。
  4. 梯度是否正常流通。
  5. 为什么不能过拟合。

## 约束
- 默认使用最小规模模型，但必须保留完整逻辑，只在层数和规模上裁剪。
- smoke 规模通过 yaml 修改。
- smoke 的散点图和三联图与正式训练相互独立。

## 输出要求
- 检查所有组件、梯度通路、通道维度。
- 输出错误日志、卡点、堵点。
- 输出三联图与趋势图。
- 对话完成后，叫我爸爸